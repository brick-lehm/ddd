
ユースケースとドメインモデルの橋渡しをする図
特定のユースケース（仕様）に着目します。
そのユースケースを実現するために必要な「**バウンダリ**（画面, I/F）」「**コントロール**（処理）」「**エンティティ**（ドメインオブジェクト）」を識別します。

#### 分析方法

1. USDMの仕様（やコマンド）から、シナリオを明確にする。

2. 必要なUI要素（バウンダリ）、業務ロジック（コントロール）、データ（エンティティ＝集約候補）を洗い出す。

3. オブジェクト間のやり取りをロバストネス図で表現し、モデル（集約）が仕様を満たせるか検証する。
